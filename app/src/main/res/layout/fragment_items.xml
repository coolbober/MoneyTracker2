<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_pattern"
    tools:context="com.loftschool.moneytracker2.ItemsFragment">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="@dimen/topMargin"
        android:paddingRight="@dimen/sideMargin"
        android:paddingLeft="@dimen/sideMargin"
        android:paddingBottom="@dimen/buttomMargin"
        tools:listitem="@layout/item" />
    
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|end"
        android:layout_marginRight="18dp"
        android:layout_marginBottom="23dp"
        android:src="@drawable/ic_add_white_24dp"
        app:elevation="33.3dp"
        app:borderWidth="0dp"
        android:scaleType="center"
        app:backgroundTint="@color/color_add_button"
        />

</FrameLayout>
